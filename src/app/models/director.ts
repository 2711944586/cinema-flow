export interface Director {
  id: number;
  name: string;
  nationality: string;
  birthYear: number;
  bio: string;
  portraitUrl: string;
  activeYears: string;
  signatureStyle: string;
  awards: string[];
  knownFor: string[];
}
